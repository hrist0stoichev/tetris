This is React Tetris app. 

## Running the app

In order to run the app you have to run these command.

* `npm install` installs all the modules from the package.json.
* `npm start` starts the React app.